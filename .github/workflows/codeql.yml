name: CodeQL Security Scan

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Security scan placeholder — nothing to do yet"
